debug := (text: string) => print(text)

hello_outer := "Hello, "
world := "world!"

main := (hello: string) string => {
  for hello print(it)
  for idx in 0...len(world) debug(world[idx])
  print("\n")
}

main(hello_outer)
